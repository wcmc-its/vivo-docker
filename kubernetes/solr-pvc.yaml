apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solr-tdb-pvc
  namespace: vivo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 30Gi